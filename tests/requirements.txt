# Testing framework for IronPython 2.7.9
# Install with: ipy -X:Frames -m pip install -r requirements.txt

# Core testing framework
ironpython-pytest

# Built-in Python libraries (available in IronPython)
# These don't need to be installed but are documented here for reference:
# - json (built-in)
# - threading (built-in) 
# - time (built-in)
# - mock (built-in in Python 2.7+)
# - unittest (built-in)
# - collections (built-in)
# - os (built-in)
# - sys (built-in)
